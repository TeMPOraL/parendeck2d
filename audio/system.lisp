(in-package #:parendeck2d.audio)

(defun initialize-audio ()
  ;; (sdl2-mixer:init)
  ;; TODO initialize and open audio device.
  )

(defun deinitialize-audio ()
  ;; TODO deinitialize audio device.
  ;; (sdl2-mixer:quit)
  )
